import { Modal } from "antd";
import styled from "styled-components";
export const ModalWrapper = styled(Modal)`
  .ant-modal-body {
    padding: 8px;
    line-height: 1;
    .field {
      margin: 0 0 8px;
    }
  }
`;
